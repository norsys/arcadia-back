sudo: required

language: node_js
node_js:
  - "7"
services:
  - docker

install:
  - npm install
script: npm test

after_success:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker build -t ymatagne/arcadia-back .
  - docker push ymatagne/arcadia-back